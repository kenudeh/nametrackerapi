{
  "build": {
    "command": "mkdir -p /app/staticfiles && python manage.py collectstatic --noinput --clear && cp -r staticfiles/* /app/staticfiles/"
  },
  "release": {
    "command": "python manage.py migrate"
  },
  "start": {
    "command": "gunicorn nametrackerapi.wsgi:application --chdir /app"
  }
}